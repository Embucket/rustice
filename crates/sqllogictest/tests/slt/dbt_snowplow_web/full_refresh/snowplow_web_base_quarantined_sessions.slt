# dbt-snowplow-web: full_refresh/snowplow_web_base_quarantined_sessions
# Source: test-dbt-snowplow-web/queries/full-refresh/base/manifest/snowplow_web_base_quarantined_sessions.sql

include ../../../fixtures/snowplow/setup.slt

statement ok
        with prep as (
        select
                cast(null as 
    TEXT
) session_identifier
        )
        select *
        from prep
        where false

# Verification (vs Snowflake on the same 200-row fixture).
# TODO(snowflake-verify): replace TODO placeholders with the row(s)
# returned by running this query against the dbt-snowplow-web tables
# in Snowflake. Numeric cells should match exactly; text cells should
# match verbatim (UUIDs, hostnames, etc.).
query I
select count(*) from embucket.public_snowplow_manifest_snowplow_manifest.snowplow_web_base_quarantined_sessions
----
TODO
