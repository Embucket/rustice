# dbt-snowplow-web: incremental/snowplow_web_users
# Source: test-dbt-snowplow-web/queries/incremental/users/snowplow_web_users.sql

include ../../../fixtures/snowplow/setup.slt

statement ok
select *
from embucket.public_snowplow_manifest_scratch.snowplow_web_users_this_run
where True --returns false if run doesn't contain new events.

# Verification (vs Snowflake on the same 200-row fixture).
# TODO(snowflake-verify): replace TODO placeholders with the row(s)
# returned by running this query against the dbt-snowplow-web tables
# in Snowflake. Numeric cells should match exactly; text cells should
# match verbatim (UUIDs, hostnames, etc.).
query TIII
select domain_userid, page_views, sessions, engaged_time_in_s from embucket.public_snowplow_manifest_derived.snowplow_web_users order by domain_userid limit 10
----
cookie_0	52	20	235
cookie_119bd174	2	2	0
cookie_466f45d	2	2	5
cookie_8cde8ba	4	4	5
cookie_d34dd17	6	6	10
