# Rustice code index

Generated by `scripts/ai/build_code_index.py`. Do not edit by hand.

## Workspace summary

- Workspace members declared in `Cargo.toml`: 13
- Default members: `crates/embucketd`
- Resolver: `2`
- Crate manifests under `crates/` but outside the declared workspace: `crates/state-store`

## Crates

| Crate | Path | Kind | Local dependencies | Features | Entry points | Tests |
|---|---|---|---|---|---|---|
| `api-snowflake-rest` | `crates/api-snowflake-rest` | lib | `api-snowflake-rest-sessions`, `catalog`, `catalog-metastore`, `error-stack`, `error-stack-trace`, `executor` | `default`, `retry-disable`, `traces-test-log` | `src/lib.rs` | `src/tests/` |
| `api-snowflake-rest-sessions` | `crates/api-snowflake-rest-sessions` | lib | `error-stack`, `error-stack-trace`, `executor` | none | `src/lib.rs` | none |
| `build-info` | `crates/build-info` | lib | none | none | `src/lib.rs`, `build.rs` | none |
| `catalog` | `crates/catalog` | lib | `catalog-metastore`, `error-stack`, `error-stack-trace`, `functions` | none | `src/lib.rs` | `src/tests/` |
| `catalog-metastore` | `crates/catalog-metastore` | lib | `error-stack`, `error-stack-trace` | none | `src/lib.rs` | none |
| `embucket-lambda` | `crates/embucket-lambda` | bin | `api-snowflake-rest`, `api-snowflake-rest-sessions`, `build-info`, `catalog-metastore`, `executor` | `default`, `retry-disable`, `streaming` | `src/main.rs` | none |
| `embucket-sqllogictest` | `crates/sqllogictest` | test-only harness | `executor` | none | `src/lib.rs`, `tests/sqllogictests.rs` | `tests/`, `[[test]] sqllogictests` |
| `embucketd` | `crates/embucketd` | bin | `api-snowflake-rest`, `api-snowflake-rest-sessions`, `build-info`, `catalog-metastore`, `executor`, `queries` | `alloc-tracing`, `default`, `queries`, `retry-disable` | `src/main.rs` | none |
| `error-stack` | `crates/error-stack` | lib | none | none | `src/lib.rs` | none |
| `error-stack-trace` | `crates/error-stack-trace` | proc-macro lib | `error-stack` | none | `src/lib.rs` | none |
| `executor` | `crates/executor` | lib | `catalog`, `catalog-metastore`, `error-stack`, `error-stack-trace`, `functions` | `default`, `test-helpers` | `src/lib.rs` | `src/tests/` |
| `functions` | `crates/functions` | lib | `error-stack`, `error-stack-trace` | `geospatial` | `src/lib.rs` | `src/tests/` |
| `queries` | `crates/queries` | lib | `error-stack`, `error-stack-trace` | `default`, `tests` | `src/lib.rs` | `src/tests.rs` |

## Local dependency graph

- `api-snowflake-rest` -> `api-snowflake-rest-sessions`, `catalog`, `catalog-metastore`, `error-stack`, `error-stack-trace`, `executor`
- `api-snowflake-rest-sessions` -> `error-stack`, `error-stack-trace`, `executor`
- `build-info` -> none
- `catalog` -> `catalog-metastore`, `error-stack`, `error-stack-trace`, `functions`
- `catalog-metastore` -> `error-stack`, `error-stack-trace`
- `embucket-lambda` -> `api-snowflake-rest`, `api-snowflake-rest-sessions`, `build-info`, `catalog-metastore`, `executor`
- `embucket-sqllogictest` -> `executor`
- `embucketd` -> `api-snowflake-rest`, `api-snowflake-rest-sessions`, `build-info`, `catalog-metastore`, `executor`, `queries`
- `error-stack` -> none
- `error-stack-trace` -> `error-stack`
- `executor` -> `catalog`, `catalog-metastore`, `error-stack`, `error-stack-trace`, `functions`
- `functions` -> `error-stack`, `error-stack-trace`
- `queries` -> `error-stack`, `error-stack-trace`

## Important execution paths

### API request to query execution

- `api-snowflake-rest/src/server/router.rs` defines the Snowflake-compatible login/query routes.
- `api-snowflake-rest/src/server/handlers.rs` accepts Axum requests and calls `handle_query_request`.
- `api-snowflake-rest/src/server/logic.rs` builds `executor::models::QueryContext` and calls `ExecutionService::query`.
- `executor/src/service.rs` owns session/query lifecycle; `executor/src/session.rs` builds the DataFusion `SessionContext` with catalog, planner, optimizer, and function registration.

### Function compatibility

- `functions/src/lib.rs` exposes `register_udfs` and re-exports aggregate registration.
- `functions/src/table/mod.rs` registers table functions where present.
- `executor/src/session.rs` calls function registration while constructing each DataFusion session.

### SQL logic tests

- `sqllogictest/tests/sqllogictests.rs` discovers `.slt` files under `crates/sqllogictest/tests/slt`.
- The harness creates local executor sessions through `executor::test_helpers::create_df_session_with_catalog_url("/dev")`.
- This path is local/offline; it does not require Snowflake for the default development loop.

## Verification commands

| Command | Source | Use |
|---|---|---|
| `make build` | `Makefile` | debug build (`cargo build`) |
| `make release` | `Makefile` | release build (`cargo build --release`) |
| `make test` | `Makefile` | workspace default test loop (`cargo test`) |
| `cargo test -p functions` | `Cargo workspace` | function crate tests |
| `cargo test -p executor` | `Cargo workspace` | executor and planner tests |
| `cargo test -p api-snowflake-rest -- --test-threads=1` | `Cargo workspace` | Snowflake-compatible REST API tests; serialized because local server tests share state |
| `cargo test -p api-snowflake-rest-sessions` | `Cargo workspace` | session/auth extraction tests |
| `cargo test -p embucket-sqllogictest` | `Cargo workspace` | offline SQL logic compatibility harness |
| `cargo test --profile=ci --workspace --all-targets --exclude api-snowflake-rest --exclude embucket-sqllogictest` | `.github/workflows/tests.yml` | main CI workspace test command |
| `cargo test --profile=ci -p api-snowflake-rest --all-targets -- --test-threads=1` | `.github/workflows/tests.yml` | main CI REST API command |
| `cargo test -p embucket-sqllogictest --profile=ci --test sqllogictests -- --test-threads $(nproc)` | `.github/workflows/tests.yml` | non-gating CI SQL logic command |
| `cargo fmt --check` | `.github/workflows/check.yml` | format check |
| `cargo clippy --all-targets --workspace` | `.github/workflows/check.yml` | workspace lint check |
| `python3 scripts/ai/build_code_index.py --check` | `this generator` | verify generated index freshness |

## Notes for agents

- Regenerate this file with `python3 scripts/ai/build_code_index.py`.
- Check freshness with `python3 scripts/ai/build_code_index.py --check`.
- Use crate-local `AGENTS.md` files for ownership and boundary notes before editing a crate.
- Use `ai/testing/test-matrix.md` for change-specific local verification choices.
- Prefer crate-level `cargo test -p ...` commands before wider workspace loops.
- `make integration-test` exists in the root Makefile, but this checkout has no root `tests/` directory; verify before relying on that target.
- Do not treat non-workspace crate manifests as build participants unless they are added to the root workspace.
